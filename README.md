# Text Input QA Agent

This agent analyzes a bug report against a Product Requirements Document (PRD) to determine if the bug is valid.

## Installation

1. Install the dependencies:

```bash
pip install -r requirements.txt
```

## Running Tests

To run the tests, execute the following command:

```bash
pytest
```
